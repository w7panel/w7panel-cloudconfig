# 配置中心的解决场景


1，当多个应用共用一些配置时，可方便统一管理，比如共用一套mysql、redis等核心数据库服务，共用一套支付接口、对象存储等服务。

2，对同一个应用的多套环境，有部分配置变化时，不需要修改整套配置数据。

3，当配置发生变化时，可快速应用到多个关联应用上。



需求1：

1，应用管理菜单下新增“配置中心”入口

2，创建配置时的设置项：

1）名称

2）下面是一个表格式的动态配置项，可增加配置表格，可使用文本方式快速配置（参考修改应用的环境变量方案）：

a）version：版本名，可添加多个版本（样式参考arco里的标签输入框），版本可为空，为空后为公共配置项

b）name：名，必填

c）value：值，可为空

d）remark：备注，可为空

3，创建完成后，配置列表中点进相应的配置名，可查看配置数据。配置数据可通过version名称点击筛选（不筛选就是全部数据）。



需求2：

1，创建/编辑配置时，新增“继承配置”项，可搜索并选择已经创建的配置名+version。选择继承配置后，可作为当前配置的最底层级数据，并展示到当前配置列表中。

2，可在选择继承配置的基础上，二次修改原配置，也可新增配置项。

3，配置新增内容变化概念，增加创建时间和更新时间，如果发生变化在1天内，当前配置更新时间会显示红色并给出“更新图标”。关联的配置项（继承者）也会受到影响，一并变更更新时间。



需求3：

1，配置详情页，新增“配置部署”tab页。可新增部署策略，将配置项应用到应用部署阶段。

2，添加部署策略时，选择已部署应用（确定到子应用的容器），分两种类型：

1）环境变量类型：可将配置项作为最底层级数据（应用本身的环境变量覆盖来自配置中心的数据，重复的会覆盖）应用到应用的环境变量上。

2）配置文件类型：可将配置项作为configmap，挂载到应用的某个路径下。

3，添加完部署策略后，不会立马执行应用到应用中，会先显示在策略列表中，每一条后面会有一个“应用”按钮。用户点击应用后才会执行部署策略并重启pod。如果当前配置存在更新状态，那么应用按钮为可用状态，如果已经应用过了，那么按钮为不可用状态。

4，点击“应用”按钮，会有一个设置页弹出层：

1）部署应用：设置页显示部署的具体应用信息

2）配置版本：可选择当前配置的版本号，并记住上一次的选择为默认选择

3）自动部署：开启/关闭，默认关闭，开启后如果当前配置存在更新状态，会自动执行应用。